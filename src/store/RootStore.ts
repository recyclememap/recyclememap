export interface IRootStore {}

export class RootStore implements IRootStore {
  // TODO: remove it when create a store
  // eslint-disable-next-line @typescript-eslint/no-empty-function
  constructor() {}
}
